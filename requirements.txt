# Clean requirements for ReputationDashboard02
# Generated from a project audit on 2026-05-20.
# Includes only packages directly imported by the codebase.

streamlit==1.32.0
pandas==2.3.3
numpy==1.26.4
scikit-learn==1.3.2
vaderSentiment==3.3.2
python-dotenv==1.2.2
psycopg[binary]

requests==2.28.1
nltk==3.9.4
joblib==1.5.3
matplotlib==3.7.2
seaborn==0.12.2
langchain-core==0.1.53
langchain-groq==0.1.3

# Notes:
# - torch, torchvision and torchaudio are installed in the environment but not directly imported in this codebase.
# - sentencepiece is installed but not directly imported; remove it unless required by future transformer model usage.
# - Keep only python-dotenv instead of separate dotenv package.
