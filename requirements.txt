# Clean requirements for ReputationDashboard02
# Generated from a project audit on 2026-05-20.
# Includes only packages directly imported by the codebase.

streamlit==1.32.0    # core Streamlit app UI
pandas==2.3.3        # data loading and processing
numpy==1.26.4        # numerical arrays and vector math
scikit-learn==1.3.2  # ML classification and feature extraction
#torch==2.2.0+cpu     # PyTorch CPU runtime
transformers==4.39.3 # transformer-based NLP models
vaderSentiment==3.3.2 # rule-based sentiment analyzer
python-dotenv==1.2.2 # .env config loading
psycopg-binary==3.3.4 # PostgreSQL driver for psycopg imports
requests==2.28.1     # HTTP client used by pipeline
nltk==3.9.4          # NLP preprocessing and sentiment support
joblib==1.5.3        # model serialization and caching
matplotlib==3.7.2    # plotting utilities
seaborn==0.12.2      # statistical visualization
langchain-core==0.1.53 # LLM chain framework used by AI insights
langchain-groq==0.1.3  # Groq connector for LangChain

# Notes:
# - torch, torchvision and torchaudio are installed in the environment but not directly imported in this codebase.
# - sentencepiece is installed but not directly imported; remove it unless required by future transformer model usage.
# - Keep only python-dotenv instead of separate dotenv package.
